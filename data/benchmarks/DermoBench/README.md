# DermoBench local release

The gated DermoBench release is stored in `release/`. It contains benchmark
task annotations and `dermobench_release_imgs.zip`; its protocol is defined in
`configs/benchmarks/dermobench/dermobench.yaml`.
