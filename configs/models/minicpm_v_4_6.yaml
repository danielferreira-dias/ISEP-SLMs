# Small local multimodal candidate model.

schema_version: 1

model:
  id: minicpm_v_4_6
  display_name: MiniCPM-V 4.6
  family: minicpm_v
  evaluation_role: candidate
  training_stage: post_trained
  task_adaptation_stage: none
  domain: general

capabilities:
  modalities:
    - text
    - image
    - video
  structured_output_modes:
    - prompt_only

source:
  type: huggingface
  repo_id: openbmb/MiniCPM-V-4.6
  revision: main
  access: public

processor:
  repo_id: openbmb/MiniCPM-V-4.6
  revision: main

backend:
  type: local
  engine: transformers

security:
  trust_remote_code: false
